export default function SuperAdminDashboard() {
    return ( <
        div className = "p-6" >
        <
        h2 className = "text-2xl font-bold" > Super Admin Dashboard < /h2> <
        p className = "mt-2 text-gray-600" > Use navbar to manage users and admins. < /p> <
        /div>
    );
}